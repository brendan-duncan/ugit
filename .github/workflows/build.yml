name: Build And Release

on:
  push:
    tags:
      - 'v*' # Trigger on version tags like v1.0.0
  workflow_dispatch: # Allow manual triggering from GitHub UI

permissions:
  contents: write # Required for creating releases

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        include:
          - os: windows-latest
            target: win
          - os: macos-latest
            target: mac
            architecture: arm64
          - os: ubuntu-latest
            target: linux

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build:prod

    - name: Build main process
      run: npm run build:main

    - name: Build and Publish (Windows / Linux)
      if: matrix.os != 'macos-latest'
      run: npm run dist:${{ matrix.target }} -- --publish always
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and Publish (macOS)
      if: matrix.os == 'macos-latest'
      run: npm run dist:${{ matrix.target }} -- --publish always
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CSC_LINK: ${{ secrets.MAC_CERTS }}
        CSC_KEY_PASSWORD: ${{ secrets.MAC_CERTS_PASSWORD }}
        APPLE_ID: ${{ secrets.APPLE_ID }}
        APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
        APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

  update-release-notes:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Update release notes and publish
        run: |
          VERSION=${{ github.ref_name }}
          VERSION_NUM=${VERSION#v}
          
          cat > /tmp/release-notes.md << EOF
          # $VERSION
          
          ## Downloads
          
          - **Windows**: [ugit-Setup-$VERSION.exe](https://github.com/${{ github.repository }}/releases/download/$VERSION/ugit-Setup-$VERSION_NUM.exe)
          - **macOS**: [ugit-$VERSION.dmg](https://github.com/${{ github.repository }}/releases/download/$VERSION/ugit-$VERSION_NUM.dmg)
          - **Linux**: [ugit-$VERSION.AppImage](https://github.com/${{ github.repository }}/releases/download/$VERSION/ugit-$VERSION_NUM.AppImage)
          EOF
          
          gh release edit $VERSION --notes-file /tmp/release-notes.md --draft=false --latest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
